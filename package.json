{
  "name": "2.0",
  "version": "1.0.0",
  "description": "Phase one of the garygermer.com redesign: a single, production-ready homepage. No other pages, templates, or CMS are part of this phase — see `ROUTE_MAP.md` for what the header/footer nav point at and why those pages don't exist yet.",
  "main": "estate-sale.js",
  "scripts": {
    "build": "eleventy && node tools/minify-assets.mjs",
    "serve": "eleventy --serve",
    "test": "echo \"Error: no test specified\" && exit 1",
    "art": "node tools/build-art-images.mjs",
    "art:check": "node tools/check-art-images.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "esbuild": "^0.28.2"
  }
}
